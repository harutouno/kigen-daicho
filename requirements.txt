# 手元で動かしている版に固定する。
#
# 上限だけ切っていたときは、本番と手元で違う版が動く可能性があった。
# 画面の書き方が版によって変わるため、「手元では出るのに本番では崩れる」を
# 追いかけることになる。動かして確かめた版をそのまま使う。
#
# なお Streamlit Cloud は、このファイルが変わると環境を作り直す。
# core/ に新しい名前を足した回は、古いモジュールを抱えたままのプロセスが
# 新しい app.py を実行して ImportError になるため、作り直しが要る。
# 本来は Manage app → Reboot app で足りる。
streamlit==1.62.0
